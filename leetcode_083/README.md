# LeetCode 83. Remove Duplicates from Sorted List

[![LeetCode](https://img.shields.io/badge/LeetCode-83-orange?style=flat-square&logo=leetcode)](https://leetcode.com/problems/remove-duplicates-from-sorted-list/)
[![Difficulty](https://img.shields.io/badge/Difficulty-Easy-brightgreen?style=flat-square)](https://leetcode.com/problems/remove-duplicates-from-sorted-list/)
[![.NET](https://img.shields.io/badge/.NET-10.0-512BD4?style=flat-square&logo=dotnet)](https://dotnet.microsoft.com/)

刪除已排序鏈結串列中的重複元素，使每個元素只出現一次。

## 📋 題目描述

給定一個**已排序**的鏈結串列的頭節點 `head`，刪除所有重複的元素，使每個元素只出現一次。返回已排序的鏈結串列。

### 範例

**範例 1:**

```
輸入: head = [1,1,2]
輸出: [1,2]
```

```
1 → 1 → 2
    ↓
1 → 2
```

**範例 2:**

```
輸入: head = [1,1,2,3,3]
輸出: [1,2,3]
```

```
1 → 1 → 2 → 3 → 3
        ↓
1 → 2 → 3
```

### 限制條件

- 鏈結串列中節點數目在範圍 `[0, 300]` 內
- `-100 <= Node.val <= 100`
- 題目保證鏈結串列已經按升序排列

## 💡 解題想法

### 核心觀察

由於鏈結串列**已經排序**，這是解題的關鍵：

> [!TIP]
> 在已排序的序列中，所有重複的元素必定相鄰。

這意味著我們不需要使用額外的資料結構（如 HashSet）來追蹤已出現的元素，只需要比較相鄰節點即可。

### 解題策略

採用**單指標遍歷**的方式：

1. 使用一個指標 `cur` 從頭節點開始遍歷
2. 比較 `cur.val` 與 `cur.next.val`
3. 若相同：跳過下一個節點（`cur.next = cur.next.next`）
4. 若不同：移動指標到下一個節點（`cur = cur.next`）

> [!IMPORTANT]
> 當發現重複並刪除節點時，**不要**移動 `cur` 指標。因為新的 `cur.next` 可能仍然是重複值，需要繼續檢查。

## 🔍 解法詳細說明

### 演算法步驟

```
步驟 1: 檢查邊界條件
        └─ 若 head 為 null，直接返回 null

步驟 2: 初始化指標
        └─ cur = head（從頭節點開始）

步驟 3: 遍歷鏈結串列
        └─ 當 cur.next 不為 null 時：
            ├─ 若 cur.val == cur.next.val
            │   └─ 刪除重複：cur.next = cur.next.next
            └─ 否則
                └─ 移動指標：cur = cur.next

步驟 4: 返回處理後的 head
```

### 程式碼實作

```csharp
public static ListNode? DeleteDuplicates(ListNode? head)
{
    // 邊界條件：若鏈結串列為空，直接回傳
    if (head is null)
    {
        return head;
    }

    // 使用 cur 指標從頭節點開始遍歷
    ListNode cur = head;

    // 持續遍歷直到沒有下一個節點
    while (cur.next is not null)
    {
        // 檢查當前節點與下一個節點的值是否相同
        if (cur.val == cur.next.val)
        {
            // 若相同，跳過下一個節點（刪除重複）
            cur.next = cur.next.next;
        }
        else
        {
            // 若不同，移動到下一個節點繼續檢查
            cur = cur.next;
        }
    }

    return head;
}
```

### 複雜度分析

| 指標 | 複雜度 | 說明 |
|------|--------|------|
| 時間複雜度 | O(n) | 遍歷鏈結串列一次，n 為節點數量 |
| 空間複雜度 | O(1) | 只使用一個指標，常數額外空間 |

## 📝 範例演示

以 `[1, 1, 2, 3, 3]` 為例，逐步說明執行過程：

### 初始狀態

```
head → [1] → [1] → [2] → [3] → [3] → null
        ↑
       cur
```

### 第 1 次迭代

```
比較: cur.val (1) == cur.next.val (1) ✓ 相同
動作: 刪除重複節點，cur.next = cur.next.next

head → [1] → [2] → [3] → [3] → null
        ↑
       cur (不移動，繼續檢查)
```

### 第 2 次迭代

```
比較: cur.val (1) != cur.next.val (2) ✗ 不同
動作: 移動指標

head → [1] → [2] → [3] → [3] → null
              ↑
             cur
```

### 第 3 次迭代

```
比較: cur.val (2) != cur.next.val (3) ✗ 不同
動作: 移動指標

head → [1] → [2] → [3] → [3] → null
                    ↑
                   cur
```

### 第 4 次迭代

```
比較: cur.val (3) == cur.next.val (3) ✓ 相同
動作: 刪除重複節點

head → [1] → [2] → [3] → null
                    ↑
                   cur
```

### 結束條件

```
cur.next == null，迴圈結束

最終結果: [1] → [2] → [3] → null
```

## 🚀 執行專案

### 前置需求

- [.NET 10 SDK](https://dotnet.microsoft.com/download) 或更高版本

### 執行步驟

```bash
# 進入專案目錄
cd leetcode_083

# 執行程式
dotnet run
```

### 預期輸出

```
Example 1: [1, 2]
Example 2: [1, 2, 3]
Example 3 (empty): []
Example 4 (single): [1]
```

## 📚 相關題目

| 題號 | 題目 | 難度 |
|------|------|------|
| [82](https://leetcode.com/problems/remove-duplicates-from-sorted-list-ii/) | Remove Duplicates from Sorted List II | Medium |
| [26](https://leetcode.com/problems/remove-duplicates-from-sorted-array/) | Remove Duplicates from Sorted Array | Easy |
| [203](https://leetcode.com/problems/remove-linked-list-elements/) | Remove Linked List Elements | Easy |
