# LeetCode 2670：找出不同元素數目差陣列

[![.NET](https://img.shields.io/badge/.NET-10.0-512BD4)](https://dotnet.microsoft.com/)
[![Language](https://img.shields.io/badge/Language-C%23-239120)](https://learn.microsoft.com/dotnet/csharp/)

這是一個使用 C# 與 .NET 10 實作的主控台專案，解答
[LeetCode 2670：Find the Distinct Difference Array](https://leetcode.com/problems/find-the-distinct-difference-array/description/)。
程式使用 `HashSet<int>` 預先計算後綴的相異元素數量，再於正向掃描時同步累積前綴的相異元素數量，
因此只需兩次線性掃描即可建立完整答案。

## 快速導覽

- [題目說明](#題目說明)
- [限制條件](#限制條件)
- [解題概念與出發點](#解題概念與出發點)
- [解法：HashSet 與後綴預處理](#解法hashset-與後綴預處理)
- [範例演示流程](#範例演示流程)
- [建置與執行](#建置與執行)
- [固定測試案例](#固定測試案例)

## 題目說明

給定一個長度為 `n`、索引從 `0` 開始的整數陣列 `nums`，建立一個同樣長度的陣列 `diff`。

對每個索引 `i`：

- 前綴為 `nums[0..i]`，包含 `nums[i]`。
- 後綴為 `nums[i+1..n-1]`，不包含 `nums[i]`。
- `diff[i]` 等於「前綴中的相異元素數量」減去「後綴中的相異元素數量」。

公式如下：

```text
diff[i]
= distinct(nums[0..i])
- distinct(nums[i+1..n-1])
```

當 `i = n - 1` 時，右側後綴是空陣列，因此其相異元素數量為 `0`。

### 輸入與輸出

- 輸入：整數陣列 `nums`。
- 輸出：整數陣列 `diff`，長度與 `nums` 相同。

例如：

```text
輸入：nums = [1, 2, 3, 4, 5]
輸出：diff = [-3, -1, 1, 3, 5]
```

索引 `0` 的前綴 `[1]` 有 `1` 種相異元素，後綴 `[2, 3, 4, 5]` 有 `4` 種，
所以 `diff[0] = 1 - 4 = -3`。

## 限制條件

- `1 <= nums.length <= 50`
- `1 <= nums[i] <= 50`

雖然限制不大，可以對每個索引重新掃描前綴與後綴，但本專案採用可擴展到較大輸入的線性解法。

## 解題概念與出發點

### 直接計算會重複做什麼？

最直觀的做法，是對每個索引 `i`：

1. 建立一個集合，掃描 `nums[0..i]`。
2. 再建立一個集合，掃描 `nums[i+1..n-1]`。
3. 將兩個集合的大小相減。

這個做法容易理解，但相鄰索引的前綴與後綴高度重疊。例如從 `i = 1` 移到 `i = 2` 時，
大部分元素已經掃描過，若全部重算便會產生重複工作。總時間複雜度會是 O(n²)。

### 如何移除重複掃描？

答案只需要兩項資訊：

1. 目前前綴 `nums[0..i]` 有多少種相異元素。
2. 下一個位置開始的後綴 `nums[i+1..n-1]` 有多少種相異元素。

前綴可以在正向走訪時持續把 `nums[i]` 加入集合；集合大小就是當前前綴的相異元素數量。
後綴則可以先由右向左預處理，將每個起點的相異元素數量記錄在 `sufCnt`。

因此：

```text
res[i] = prefixSet.Count - sufCnt[i + 1]
```

## 解法：HashSet 與後綴預處理

### 使用的資料結構

- `HashSet<int> set`
  - 集合只保留不重複的值。
  - `set.Count` 可以直接表示目前區段的相異元素數量。
- `int[] sufCnt`
  - 長度為 `n + 1`。
  - `sufCnt[k]` 表示 `nums[k..n-1]` 的相異元素數量。
  - `sufCnt[n] = 0` 表示陣列尾端之後的空後綴。
- `int[] res`
  - 儲存每個索引最後的相異元素數量差。

### 第一階段：由右向左預處理後綴

從 `n - 1` 走到 `1`，把每個元素加入 `HashSet`，並將集合大小寫入 `sufCnt[i]`。

程式不需要計算 `sufCnt[0]`，因為計算 `res[i]` 時只會查詢 `sufCnt[i + 1]`；
當 `i` 的範圍是 `0..n-1`，實際使用的後綴索引就是 `1..n`。

### 第二階段：由左向右累積前綴

清空集合後，從索引 `0` 開始：

1. 將 `nums[i]` 加入集合。
2. `set.Count` 是 `nums[0..i]` 的相異元素數量。
3. `sufCnt[i + 1]` 是 `nums[i+1..n-1]` 的相異元素數量。
4. 兩者相減並寫入 `res[i]`。

### 為什麼這個解法正確？

對任意索引 `i`：

- 正向迴圈處理完 `nums[i]` 後，集合恰好包含 `nums[0..i]` 出現過的所有不同值，
  所以 `set.Count` 等於前綴的相異元素數量。
- 反向預處理已保證 `sufCnt[i + 1]` 等於 `nums[i+1..n-1]` 的相異元素數量。
- 程式將這兩個定義完全相符的數量相減，因此得到的 `res[i]` 正是題目要求的 `diff[i]`。

以上敘述對所有 `0 <= i < n` 都成立，所以回傳陣列的每個位置皆正確。

### 複雜度分析

- 時間複雜度：O(n)
  - 反向預處理走訪一次。
  - 正向建立答案再走訪一次。
  - `HashSet` 的新增操作平均為 O(1)。
- 空間複雜度：O(n)
  - `sufCnt` 與答案陣列各需要 O(n)。
  - `HashSet` 最多保存 O(n) 個不同值。

## 範例演示流程

以官方範例二為例：

```text
nums = [3, 2, 3, 4, 2]
```

### 1. 建立後綴相異數量

`sufCnt[k]` 記錄從索引 `k` 到陣列尾端的相異元素數量：

| `k` | 後綴 `nums[k..n-1]` | 相異元素集合 | `sufCnt[k]` |
|---:|---|---|---:|
| 5 | `[]` | `{}` | 0 |
| 4 | `[2]` | `{2}` | 1 |
| 3 | `[4, 2]` | `{4, 2}` | 2 |
| 2 | `[3, 4, 2]` | `{3, 4, 2}` | 3 |
| 1 | `[2, 3, 4, 2]` | `{2, 3, 4}` | 3 |

`sufCnt[0]` 不會被答案公式使用，因此程式不必計算它。

### 2. 正向建立答案

| `i` | 加入的值 | 前綴相異集合 | 前綴數量 | 使用的後綴數量 | 計算 | `res[i]` |
|---:|---:|---|---:|---:|---|---:|
| 0 | 3 | `{3}` | 1 | `sufCnt[1] = 3` | `1 - 3` | -2 |
| 1 | 2 | `{3, 2}` | 2 | `sufCnt[2] = 3` | `2 - 3` | -1 |
| 2 | 3 | `{3, 2}` | 2 | `sufCnt[3] = 2` | `2 - 2` | 0 |
| 3 | 4 | `{3, 2, 4}` | 3 | `sufCnt[4] = 1` | `3 - 1` | 2 |
| 4 | 2 | `{3, 2, 4}` | 3 | `sufCnt[5] = 0` | `3 - 0` | 3 |

最後得到：

```text
res = [-2, -1, 0, 2, 3]
```

這個例子也呈現了 `HashSet` 的重要特性：索引 `2` 再次遇到 `3`、索引 `4` 再次遇到 `2` 時，
集合大小不會增加，因為相同值只計算一次。

## 建置與執行

需求：

- [.NET 10 SDK](https://dotnet.microsoft.com/download/dotnet/10.0)

請在此 repository 目錄執行：

```powershell
dotnet build .\leetcode_2670\leetcode_2670.csproj
dotnet run --project .\leetcode_2670\leetcode_2670.csproj
```

## 固定測試案例

目前沒有獨立的測試專案或測試框架。`Main` 會執行四組固定案例，
使用 `SequenceEqual` 比較實際與預期陣列的每個元素，並印出明確的 PASS/FAIL：

| 案例 | 用途 |
|---|---|
| 官方範例一 | 驗證所有元素皆不同時，前綴增加且後綴遞減的結果 |
| 官方範例二 | 驗證重複元素不會重複計數 |
| 單一元素 | 驗證最小長度及空後綴 |
| 全部重複 | 驗證集合大小在重複輸入下維持不變 |

實際執行輸出：

```text
LeetCode 2670 - Find the Distinct Difference Array

[PASS] 官方範例一
  Input:    [1, 2, 3, 4, 5]
  Expected: [-3, -1, 1, 3, 5]
  Actual:   [-3, -1, 1, 3, 5]

[PASS] 官方範例二
  Input:    [3, 2, 3, 4, 2]
  Expected: [-2, -1, 0, 2, 3]
  Actual:   [-2, -1, 0, 2, 3]

[PASS] 單一元素
  Input:    [1]
  Expected: [1]
  Actual:   [1]

[PASS] 全部重複
  Input:    [5, 5, 5]
  Expected: [0, 0, 1]
  Actual:   [0, 0, 1]

Result: 4/4 passed.
```

若任何案例失敗，程式除了顯示 `[FAIL]`，也會設定非零結束碼，方便命令列或自動化流程偵測失敗。

## 專案結構

```text
.
├── README.md
├── docs/
│   └── readme-template.md
└── leetcode_2670/
    ├── leetcode_2670.csproj
    └── Program.cs
```

- `leetcode_2670/Program.cs`：題目解法、固定測試 runner 與主程式進入點。
- `leetcode_2670/leetcode_2670.csproj`：.NET 10 主控台專案設定。
- `docs/readme-template.md`：初次建立 README 時使用的內容與驗證指引。
