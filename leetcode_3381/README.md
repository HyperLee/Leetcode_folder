# leetcode_3381 project

This repository uses the following coding style settings (set in `.editorconfig`):

- `insert_final_newline = true` for all files
- `charset = utf-8-bom` for common source files and dotfiles (.editorconfig, .gitignore, .gitattributes)

This file was added to document the changes made to `.editorconfig`.
