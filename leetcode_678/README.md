# 678. Valid Parenthesis String

[![LeetCode](https://img.shields.io/badge/LeetCode-678-orange?style=flat-square)](https://leetcode.com/problems/valid-parenthesis-string/)
[![Difficulty](https://img.shields.io/badge/Difficulty-Medium-yellow?style=flat-square)](https://leetcode.com/problems/valid-parenthesis-string/)
[![.NET](https://img.shields.io/badge/.NET-10.0-512BD4?style=flat-square)](https://dotnet.microsoft.com/)

使用 C# 實作「有效的括號字串」，並以雙索引堆疊、貪心上下界與動態規劃三種方法比較不同的狀態表示方式。`Main` 內含十組可直接執行的案例，會自動驗證三種解法的結果。

## 題目說明

給定一個只包含 `(`、`)` 與 `*` 的字串 `s`，判斷是否能把每個 `*` 分別視為以下任一內容，使整個字串成為有效括號字串：

- 一個左括號 `(`。
- 一個右括號 `)`。
- 空字串。

有效字串必須符合：

1. 每個左括號都有對應的右括號。
2. 每個右括號都有對應的左括號。
3. 左括號必須出現在與它配對的右括號之前。

官方範例：

| 輸入 | 輸出 | 一種可行解釋 |
|---|---:|---|
| `()` | `true` | 原字串已有效 |
| `(*)` | `true` | 將 `*` 視為空字串 |
| `(*))` | `true` | 將 `*` 視為左括號 |

## 限制條件

- `1 <= s.length <= 100`
- `s[i]` 只會是 `(`、`)` 或 `*`
- 本專案依題目契約處理合法且非空的輸入，不額外驗證 `null`、空字串或其他字元。

## 解題概念與出發點

### `*` 帶來的是「多種可能狀態」

一般括號題只需追蹤目前尚未配對的左括號數；遇到 `(` 加一，遇到 `)` 減一，而且任何前綴都不能降到負數。

本題的 `*` 可以讓未配對左括號數增加一、減少一或維持不變，因此掃描到同一位置時可能同時存在多個合法狀態。三種解法的差異，就在於如何保存或壓縮這些狀態：

- 雙堆疊保存可供配對的實際索引。
- 貪心只保存所有可能括號數的最小值與最大值。
- 動態規劃保存每個前綴所有可達的括號數。

### 為什麼不能只比較括號總數

有效括號除了數量相等，還要求順序正確。例如 `)(` 含有相同數量的左右括號，但第一個右括號出現時，前方沒有任何左括號可配對，因此無效。

相同限制也適用於星號。字串 `(*(` 中雖然可把 `*` 當成右括號，但該星號位於最後一個左括號之前，無法補足結尾的左括號。每種解法都必須保證任一前綴不會出現無法配對的右括號。

## 解法一：雙索引堆疊

### 設計說明

`CheckValidString` 使用兩個 `Stack<int>`：

- `leftParentheses` 保存尚未配對的左括號索引。
- `wildcards` 保存尚未使用的星號索引。

由左至右掃描：

1. 遇到 `(`，將索引放入左括號堆疊。
2. 遇到 `*`，將索引放入星號堆疊。
3. 遇到 `)`，優先彈出真正的左括號；若沒有左括號，再把一個較早的星號當成左括號。
4. 兩個堆疊都為空卻遇到 `)`，代表目前前綴不可能有效，立即回傳 `false`。
5. 掃描結束後，將剩餘星號當成右括號，逐一配對剩餘左括號。

最後一步必須比較索引。只有位於左括號之後的星號，才能當作它的右括號；若左括號索引大於星號索引，就不能形成合法配對。

### 正確性重點

- 遇到右括號時優先使用真正的左括號，可以保留用途較有彈性的星號。
- 堆疊頂端是目前最右側的未配對位置；從右向左配對剩餘左括號與星號，可讓最接近的合法位置先配對。
- 最後沒有剩餘左括號時，未使用的星號都可視為空字串，因此結果有效。

### 複雜度

| 項目 | 複雜度 | 說明 |
|---|---:|---|
| 時間 | `O(n)` | 每個索引最多被放入與取出堆疊各一次。 |
| 輔助空間 | `O(n)` | 最差情況需保存所有左括號或星號索引。 |

### 範例演示：`(*))`

| 索引 | 字元 | 左括號堆疊 | 星號堆疊 | 動作 |
|---:|:---:|---|---|---|
| 0 | `(` | `[0]` | `[]` | 保存左括號索引 |
| 1 | `*` | `[0]` | `[1]` | 保存星號索引 |
| 2 | `)` | `[]` | `[1]` | 優先與索引 0 的左括號配對 |
| 3 | `)` | `[]` | `[]` | 沒有左括號，將索引 1 的星號視為左括號 |

掃描結束後兩個堆疊皆空，因此回傳 `true`。

## 解法二：貪心上下界

### 設計說明

`CheckValidString2` 不保存每一種星號替換結果，而是維護目前所有可能狀態的範圍：

- `minimumOpen`：最少可能有幾個未配對左括號。
- `maximumOpen`：最多可能有幾個未配對左括號。

每個字元的更新規則：

| 字元 | `minimumOpen` | `maximumOpen` | 原因 |
|:---:|---:|---:|---|
| `(` | `+1` | `+1` | 一定新增一個未配對左括號 |
| `)` | `-1` | `-1` | 一定需要配對一個左括號 |
| `*` | `-1` | `+1` | 可視為右括號、空字串或左括號 |

更新後還需要兩個判斷：

1. `maximumOpen < 0`：即使把先前所有星號都當成左括號，右括號仍然過多，立即回傳 `false`。
2. `minimumOpen < 0`：實際未配對左括號數不可能為負，因此將下界修正為 0；這等同選擇把部分星號視為空字串。

掃描結束時，若 `minimumOpen == 0`，代表零個未配對左括號仍是可達狀態。

### 正確性重點

對每個前綴而言，可達的未配對左括號數形成一段連續範圍，因此只需保留兩端。上下界更新完整涵蓋 `*` 的三種用途，而 `maximumOpen < 0` 能立即排除右括號過多的前綴。最後下界為零，就代表至少存在一條選擇路徑能配對所有左括號。

### 複雜度

| 項目 | 複雜度 | 說明 |
|---|---:|---|
| 時間 | `O(n)` | 字串只掃描一次。 |
| 輔助空間 | `O(1)` | 只保存兩個整數上下界。 |

### 範例演示：`(*))`

| 處理後前綴 | 字元 | `minimumOpen` | `maximumOpen` | 判斷 |
|---|:---:|---:|---:|---|
| `(` | `(` | 1 | 1 | 一定新增左括號 |
| `(*` | `*` | 0 | 2 | 星號可減少、維持或增加可能數量 |
| `(*)` | `)` | 0 | 1 | 下界原為 -1，修正為 0 |
| `(*))` | `)` | 0 | 0 | 存在全部配對完成的選擇 |

最後 `minimumOpen == 0`，因此回傳 `true`。

## 解法三：動態規劃

### 狀態定義

`CheckValidString3` 建立二維布林表：

`reachable[i, open]` 表示處理完前 `i` 個字元後，是否可能留下 `open` 個尚未配對的左括號。

初始狀態是 `reachable[0, 0] = true`，代表尚未處理字元時沒有未配對括號。

### 狀態轉移

只從目前可達的狀態轉移：

- `(`：轉移到 `open + 1`。
- `)`：當 `open > 0` 時，轉移到 `open - 1`。
- `*`：
  - 視為左括號，轉移到 `open + 1`。
  - 視為空字串，維持 `open`。
  - 視為右括號，當 `open > 0` 時轉移到 `open - 1`。

不能讓 `open` 變成負數，這項限制保證任何被保存的狀態，其每個前綴都有足夠的左括號配對右括號。最終答案為 `reachable[n, 0]`。

### 正確性重點

每次轉移都逐一列出目前字元的所有合法解釋，因此不會漏掉任何星號替換方式；同時只從可達狀態前進，且禁止負的未配對數量，所以不會加入順序錯誤的選擇。最後狀態 0 可達，恰好表示存在一種替換方式能配對全部括號。

### 複雜度

| 項目 | 複雜度 | 說明 |
|---|---:|---|
| 時間 | `O(n²)` | 每個字元檢查最多 `n + 1` 種未配對數量。 |
| 輔助空間 | `O(n²)` | 二維表保存所有前綴與未配對數量。 |

### 範例演示：`(*))`

以下集合列出每個前綴可達的 `open`：

| 已處理前綴 | 可達的未配對左括號數 | 狀態來源 |
|---|---|---|
| 空字串 | `{0}` | 初始狀態 |
| `(` | `{1}` | 左括號使數量加一 |
| `(*` | `{0, 1, 2}` | 星號分別視為右括號、空字串、左括號 |
| `(*)` | `{0, 1}` | 右括號只能從正數狀態減一 |
| `(*))` | `{0}` | 最後一個右括號完成剩餘配對 |

最終集合包含 0，因此回傳 `true`。

## 三種解法比較

| 解法 | 方法 | 時間 | 輔助空間 | 特色 |
|---|---|---:|---:|---|
| 雙索引堆疊 | `CheckValidString` | `O(n)` | `O(n)` | 保留實際位置，索引順序容易直接解釋 |
| 貪心上下界 | `CheckValidString2` | `O(n)` | `O(1)` | 將所有可能狀態壓縮成連續範圍，效率最佳 |
| 動態規劃 | `CheckValidString3` | `O(n²)` | `O(n²)` | 明確保存全部可達狀態，最適合理解完整選擇空間 |

若只考慮正式解題效率，貪心解法在時間與空間上最有優勢；雙堆疊解法適合強調括號位置順序；動態規劃則提供最直接、完整的狀態枚舉模型。

## 可執行驗證

`Main` 內含十組固定案例，涵蓋：

- 三個官方範例。
- 星號單獨作為空字串。
- 單側括號缺失。
- 星號索引能否合法補足右括號。
- 無法由後續星號修復的錯誤前綴。
- 長度 100 的全星號上限案例。

每組案例都會呼叫三種解法，因此總共執行 30 個 checks。任一結果不符預期時，程式會輸出 `FAIL` 並設定非零結束碼。

## 建置與執行

環境需求：[.NET 10 SDK](https://dotnet.microsoft.com/download)

從本 README 所在的 repository 根目錄執行：

```bash
dotnet restore leetcode_678/leetcode_678.csproj
dotnet build leetcode_678/leetcode_678.csproj --nologo --no-restore
dotnet run --no-build --project leetcode_678/leetcode_678.csproj
```

此 repository 目前沒有自動化測試專案；驗收方式是成功建置，再執行 `Main` 中會自行比較預期值的固定案例。

### 實際執行結果

以下內容來自 `dotnet run --no-build --project leetcode_678/leetcode_678.csproj`：

```text
Case 1: 基本有效括號
  input:    "()"
  expected: True
  雙堆疊: True (PASS)
  貪心: True (PASS)
  動態規劃: True (PASS)

Case 2: 星號視為空字串
  input:    "(*)"
  expected: True
  雙堆疊: True (PASS)
  貪心: True (PASS)
  動態規劃: True (PASS)

Case 3: 星號視為左括號
  input:    "(*))"
  expected: True
  雙堆疊: True (PASS)
  貪心: True (PASS)
  動態規劃: True (PASS)

Case 4: 單一星號
  input:    "*"
  expected: True
  雙堆疊: True (PASS)
  貪心: True (PASS)
  動態規劃: True (PASS)

Case 5: 缺少右括號
  input:    "("
  expected: False
  雙堆疊: False (PASS)
  貪心: False (PASS)
  動態規劃: False (PASS)

Case 6: 缺少左括號
  input:    ")"
  expected: False
  雙堆疊: False (PASS)
  貪心: False (PASS)
  動態規劃: False (PASS)

Case 7: 星號補足右括號
  input:    "((*)"
  expected: True
  雙堆疊: True (PASS)
  貪心: True (PASS)
  動態規劃: True (PASS)

Case 8: 結尾仍有左括號
  input:    "(*("
  expected: False
  雙堆疊: False (PASS)
  貪心: False (PASS)
  動態規劃: False (PASS)

Case 9: 錯誤前綴無法補救
  input:    "())*"
  expected: False
  雙堆疊: False (PASS)
  貪心: False (PASS)
  動態規劃: False (PASS)

Case 10: 長度上限的全星號
  input:    "****************************************************************************************************"
  expected: True
  雙堆疊: True (PASS)
  貪心: True (PASS)
  動態規劃: True (PASS)

Summary: 30/30 checks passed.
```

## 專案結構

| 路徑 | 用途 |
|---|---|
| `leetcode_678/Program.cs` | 三種解法、固定案例與 console acceptance harness |
| `leetcode_678/leetcode_678.csproj` | .NET 10 console 專案設定 |
| `docs/readme-template.md` | README 的內容與驗證原則 |

## 參考資料

- [LeetCode 678 — Valid Parenthesis String](https://leetcode.com/problems/valid-parenthesis-string/)
- [力扣 678 — 有效的括號字符串](https://leetcode.cn/problems/valid-parenthesis-string/)
