# LeetCode 3568 — 清理教室的最少移動

![.NET](https://img.shields.io/badge/.NET-10.0-512BD4?logo=dotnet)
![Language](https://img.shields.io/badge/Language-C%23-239120?logo=csharp)

使用 C# 實作 **BFS（廣度優先搜尋）＋Bitmask（位元遮罩）＋支配剪枝**，求出在能量限制下清理教室內所有垃圾所需的最少移動次數。

- 題目：[3568. Minimum Moves to Clean the Classroom](https://leetcode.com/problems/minimum-moves-to-clean-the-classroom/)
- 專案：`.NET 10.0` 主控台應用程式
- 解法入口：`MinMoves(string[] classroom, int energy)`

## 目錄

- [題目說明](#題目說明)
- [限制條件](#限制條件)
- [解題概念與出發點](#解題概念與出發點)
- [解法：BFS＋Bitmask＋支配剪枝](#解法bfsbitmask支配剪枝)
- [範例演示流程](#範例演示流程)
- [正確性說明](#正確性說明)
- [複雜度分析](#複雜度分析)
- [建置與執行](#建置與執行)

## 題目說明

給定一個 `m × n` 的教室網格 `classroom`。學生從起點出發，必須收集所有垃圾；每移動到相鄰格會消耗一點能量，進入重置區域時則會恢復成最大能量。請回傳完成清理的最少移動次數；若無法完成則回傳 `-1`。

每個字元的意義如下：

| 字元 | 意義 |
| --- | --- |
| `S` | 唯一的學生起點，出發時能量為 `energy` |
| `L` | 必須收集的垃圾；收集後再次經過不會重複計算 |
| `R` | 重置區域；進入後能量恢復成 `energy`，可以重複使用 |
| `X` | 無法進入的障礙物 |
| `.` | 可以通行的空白格 |

學生每次只能向上、下、左、右移動一格，而且每次移動的成本都是一樣的：一個步數與一點能量。

## 限制條件

- `1 <= m == classroom.Length <= 20`
- `1 <= n == classroom[i].Length <= 20`
- `classroom[i][j]` 只會是 `S`、`L`、`R`、`X` 或 `.`
- `1 <= energy <= 50`
- 網格中恰好有一個 `S`
- 網格中最多有十個 `L`

垃圾最多只有十個，因此最多只有 `2^10 = 1024` 種「已清理垃圾集合」，這讓 Bitmask 狀態壓縮可行。

## 解題概念與出發點

### 為什麼選擇 BFS？

題目要求的是最少移動次數，而且每次移動的成本固定為一。BFS 會依照步數逐層展開：

```text
第 0 層：走 0 步可到達的狀態
第 1 層：走 1 步可到達的狀態
第 2 層：走 2 步可到達的狀態
...
```

因此，第一次從 Queue 取出「所有垃圾都已清理」的狀態時，該狀態的 `steps` 就是全域最少移動次數。

### 為什麼不能只記錄位置？

普通迷宮常使用 `visited[x, y]`，但這題即使站在同一格，未來能否完成仍會受到兩件事影響：

1. 已經清理了哪些垃圾。
2. 還剩多少能量。

例如以下狀態雖然位置相同，後續能力卻完全不同：

```text
(x, y, mask = 001, energy = 1)
(x, y, mask = 101, energy = 1)
(x, y, mask = 001, energy = 4)
```

因此完整搜尋狀態必須包含：

```text
(位置 x, 位置 y, 已清理集合 mask, 剩餘能量 e, 已走步數 steps)
```

### 為什麼使用 Bitmask？

掃描網格時，程式按照列優先順序替每個垃圾分配一個 bit：

```text
第 0 個垃圾：001
第 1 個垃圾：010
第 2 個垃圾：100
```

假設目前 `mask = 001`，下一步走到 bit 為 `010` 的垃圾，使用 OR 運算即可更新集合：

```text
001
OR 010
------
011
```

程式中的更新方式是：

```csharp
int nmask = t.mask | id[nx, ny];
```

非垃圾格的 `id` 預設為零，因此 OR 後不會改變 `mask`；再次走到已收集的垃圾時，相同的 bit 也不會被重複計算。

## 解法：BFS＋Bitmask＋支配剪枝

### 1. 掃描起點並替垃圾編號

程式先找出起點 `(sx, sy)`，並把每個垃圾對應到 `id[x, y] = 1 << cnt`。

若共有 `L` 個垃圾：

```text
full = 1 << L
完成狀態 = full - 1
```

例如兩個垃圾時，`full = 100₂`，而 `full - 1 = 11₂`，代表兩個垃圾都已清理。

### 2. 設計 `bestEnergy`

程式不是使用四維的 `visited[x, y, mask, energy]`，而是建立：

```csharp
bestEnergy[x, y, mask]
```

它表示：

> 到達 `(x, y)` 且已清理集合為 `mask` 時，目前曾看過的最高剩餘能量。

陣列初始值設為 `-1`，因為合法剩餘能量可能是零，不能用零代表「尚未到達」。起點狀態則是：

```text
位置   = (sx, sy)
mask   = 0
energy = 最大能量
steps  = 0
```

### 3. 從 Queue 逐層展開

Queue 中每筆資料為：

```csharp
(int x, int y, int mask, int e, int steps)
```

每次取出狀態後依序處理：

1. 若 `mask == full - 1`，立即回傳 `steps`。
2. 若 `e == 0` 且尚未完成，便無法再移動，停止展開此狀態。
3. 嘗試上下左右四個方向。
4. 排除超出網格或位於 `X` 的位置。
5. 計算進入下一格後的能量與垃圾集合。
6. 只有新狀態未被既有狀態支配時，才加入 Queue。

完成判斷位於能量判斷之前非常重要：學生可能用最後一點能量走到最後一個垃圾，此時剩餘能量雖然為零，清理任務仍然已完成。

### 4. 處理能量與重置區域

移動到普通格時：

```text
新能量 = 原能量 - 1
```

移動到 `R` 時，這一步仍計入移動次數，但抵達後立刻補滿：

```text
新能量 = energy
```

因此程式使用：

```csharp
int ne = classroom[nx][ny] == 'R' ? energy : t.e - 1;
```

### 5. 支配剪枝

假設兩條路線抵達相同位置，而且清理過的垃圾集合也相同：

```text
A = (x, y, mask = 011, energy = 5)
B = (x, y, mask = 011, energy = 2)
```

從 B 能走的任何後續路徑，A 都能走，而且 A 可能走得更遠。因此 A 完全支配 B，B 沒有繼續搜尋的價值。

程式只在以下條件成立時保留新狀態：

```csharp
if (ne > bestEnergy[nx, ny, nmask])
```

這項剪枝把原本直觀的四維狀態壓縮成：

```text
(x, y, mask) → 只保存目前最高 energy
```

若 BFS 用盡所有可行狀態仍沒有完成，便回傳 `-1`。

## 範例演示流程

以下使用官方第二個範例，因為它同時涵蓋多個垃圾、Bitmask 更新與能量重置。

```text
classroom = ["LS", "RL"]
energy = 4
```

網格與垃圾編號如下：

```text
(0,0) L0 = 01    (0,1) S
(1,0) R          (1,1) L1 = 10
```

共有兩個垃圾，因此完成 mask 為 `11`。

| 階段 | 位置 | 格子 | 步數 | 剩餘能量 | mask | 說明 |
| --- | --- | --- | ---: | ---: | --- | --- |
| 起點 | `(0,1)` | `S` | 0 | 4 | `00` | 尚未清理垃圾 |
| 移動 1 | `(0,0)` | `L0` | 1 | 3 | `01` | `00 OR 01 = 01`，收集第一個垃圾 |
| 移動 2 | `(1,0)` | `R` | 2 | 4 | `01` | 進入重置區域，能量恢復為 4 |
| 移動 3 | `(1,1)` | `L1` | 3 | 3 | `11` | `01 OR 10 = 11`，所有垃圾完成 |

BFS 第一次取得 `mask = 11` 的狀態時回傳 `3`，因此答案是三步。

另外兩個執行案例分別驗證：

- `範例 1`：用最後一點能量到達唯一垃圾，答案為 `2`。
- `範例 3`：障礙物與能量限制使兩個垃圾無法在同一路徑中全部收集，答案為 `-1`。

## 正確性說明

1. **垃圾集合正確**：每個垃圾擁有唯一 bit，OR 運算只會加入已抵達的垃圾，不會移除或重複計算垃圾。
2. **移動狀態完整**：Queue 同時保存位置、垃圾集合、剩餘能量與步數，足以決定所有合法後續行動。
3. **剪枝安全**：在位置及 `mask` 相同時，較高能量狀態能執行較低能量狀態的全部後續移動，因此捨棄後者不會失去更佳答案。
4. **最短步數正確**：所有移動成本都為一，BFS 按步數遞增處理狀態，所以第一次完成所有垃圾時必定是最少移動次數。
5. **無解判斷正確**：若 Queue 清空，代表所有未被安全剪枝的可行狀態都已檢查，仍未完成時即不存在合法路徑。

## 複雜度分析

令：

- `m` 為列數。
- `n` 為欄數。
- `L` 為垃圾數量。
- `E` 為最大能量。

`mask` 共有 `2^L` 種可能。對同一個 `(x, y, mask)`，最高能量可能隨搜尋被提高，最保守可依能量範圍估計更新次數，因此：

- 時間複雜度：`O(m × n × 2^L × E)`。
- `bestEnergy` 空間複雜度：`O(m × n × 2^L)`。

每個狀態最多嘗試四個方向，常數四不影響大 O 表示法。

## 建置與執行

請在本 README 所在的 `leetcode_3568` 目錄執行。

### 建置

```powershell
dotnet build .\leetcode_3568\leetcode_3568.csproj --nologo
```

驗證結果為建置成功，零警告、零錯誤。

### 執行固定案例

```powershell
dotnet run --project .\leetcode_3568\leetcode_3568.csproj --no-build
```

實際輸出：

```text
範例 1：PASS｜預期：2｜實際：2
範例 2：PASS｜預期：3｜實際：3
範例 3：PASS｜預期：-1｜實際：-1
總結：3/3 通過
```

## 專案結構

```text
leetcode_3568/
├── README.md
├── docs/
│   └── readme-template.md
└── leetcode_3568/
    ├── leetcode_3568.csproj
    └── Program.cs
```
