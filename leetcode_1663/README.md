# LeetCode 1663: 具有給定數值的最小字串

[![LeetCode](https://img.shields.io/badge/LeetCode-1663-orange?style=flat-square&logo=leetcode)](https://leetcode.com/problems/smallest-string-with-a-given-numeric-value/)
[![Difficulty](https://img.shields.io/badge/Difficulty-Medium-yellow?style=flat-square)](https://leetcode.com/problems/smallest-string-with-a-given-numeric-value/)
[![Language](https://img.shields.io/badge/Language-C%23-239120?style=flat-square&logo=csharp)](https://docs.microsoft.com/dotnet/csharp/)

## 📋 題目說明

給定兩個整數 `n` 和 `k`，回傳一個長度為 `n` 且字母數值總和等於 `k` 的字串。在所有滿足條件的字串中，回傳**字典序（lexicographically）最小**的那一個。

### 字母數值定義

小寫字母的數值定義為其在字母表中的位置：
- `a = 1`, `b = 2`, `c = 3`, ..., `z = 26`

字串的數值為其所有字母數值的總和。

### 範例

| 範例 | 輸入 | 輸出 | 說明 |
|------|------|------|------|
| 1 | n = 3, k = 27 | `"aay"` | 1 + 1 + 25 = 27 |
| 2 | n = 5, k = 73 | `"aaszz"` | 1 + 1 + 19 + 26 + 26 = 73 |

### 限制條件

- `1 <= n <= 10^5`
- `n <= k <= 26 * n`

---

## 💡 解題概念與出發點

### 核心思路：貪心演算法（Greedy Algorithm）

要讓字串的**字典序最小**，關鍵在於：

> **盡可能讓前面的字元為 'a'，將較大的字元放在後面。**

這是因為字典序比較時，會從左到右逐一比較字元。例如 `"aaz"` 比 `"aba"` 小，因為第二個字元 `a < b`。

### 解題策略

1. **從後往前填充**：由於要讓前面盡量小，我們從字串末尾開始決定每個位置的字元
2. **能放 'z' 就放 'z'**：如果剩餘數值足夠，後面的位置應該盡量放大字元
3. **確保前面能放 'a'**：在放置字元時，必須確保剩餘的位置至少能放 'a'（每個 'a' 需要數值 1）

---

## 🔧 演算法詳解

### 判斷條件

對於當前位置（從後往前第 i 個），需要判斷是否能放 'z'：

```
如果 k - 26 >= (剩餘位置數 - 1)
    → 可以放 'z'，因為減去 26 後，剩餘數值仍足夠讓前面的位置都放 'a'
否則
    → 計算當前位置應該放的字元，使前面的位置都能放 'a'
```

### 計算中間字元

當無法放 'z' 時，需要計算當前位置應放的字元：

```
假設前面 n-1 個位置都放 'a'，需要數值 = n - 1
當前位置需要的數值 = k - (n - 1) = k - n + 1
對應的字元 = 'a' + (k - n + 1 - 1) = 'a' + (k - n)
```

### 演算法流程圖

```
開始
  ↓
建立長度為 n 的字元陣列
  ↓
從最後一個位置往前遍歷
  ↓
┌─────────────────────────────┐
│ k - 26 >= n - 1 ?           │
│   是 → 放 'z', k -= 26      │
│   否 → 放 'a' + (k-n), k=n-1│
└─────────────────────────────┘
  ↓
n--, 繼續下一個位置
  ↓
n == 0 ?
  是 → 回傳字串
  否 → 返回迴圈
```

---

## 📝 範例演示

### 範例：n = 5, k = 73

**目標**：建立長度為 5，數值總和為 73 的字典序最小字串。

| 步驟 | 位置 (index) | 剩餘位置 (n) | 剩餘數值 (k) | 判斷條件 | 放置字元 | 說明 |
|------|-------------|-------------|-------------|----------|----------|------|
| 1 | 4 | 5 | 73 | 73-26=47 >= 4 ✓ | 'z' | 可以放 z |
| 2 | 3 | 4 | 47 | 47-26=21 >= 3 ✓ | 'z' | 可以放 z |
| 3 | 2 | 3 | 21 | 21-26=-5 < 2 ✗ | 's' | 'a'+(21-3)='s'(19) |
| 4 | 1 | 2 | 2 | 2-26=-24 < 1 ✗ | 'a' | 剩餘數值=n |
| 5 | 0 | 1 | 1 | 1-26=-25 < 0 ✗ | 'a' | 剩餘數值=n |

**結果**：`"aaszz"`

**驗證**：1 + 1 + 19 + 26 + 26 = **73** ✓

### 步驟詳解圖示

```
初始狀態: [ ][ ][ ][ ][ ]   k = 73, n = 5

步驟 1:   [ ][ ][ ][ ][z]   k = 47, n = 4  (73 - 26 = 47)
步驟 2:   [ ][ ][ ][z][z]   k = 21, n = 3  (47 - 26 = 21)
步驟 3:   [ ][ ][s][z][z]   k = 2,  n = 2  (放 's'=19, 剩餘 21-19=2)
步驟 4:   [ ][a][s][z][z]   k = 1,  n = 1  (放 'a'=1)
步驟 5:   [a][a][s][z][z]   k = 0,  n = 0  (放 'a'=1)

最終結果: "aaszz"
```

---

## 🚀 執行方式

### 環境需求

- [.NET 10 SDK](https://dotnet.microsoft.com/download) 或更新版本

### 建構與執行

```bash
# 進入專案目錄
cd leetcode_1663

# 建構專案
dotnet build

# 執行程式
dotnet run
```

### 預期輸出

```
n = 3, k = 27 => "aay"
n = 5, k = 73 => "aaszz"
n = 3, k = 3 => "aaa"
n = 1, k = 26 => "z"
n = 2, k = 52 => "zz"
```

---

## 📊 複雜度分析

| 複雜度類型 | 值 | 說明 |
|-----------|-----|------|
| 時間複雜度 | O(n) | 只需遍歷一次字串的每個位置 |
| 空間複雜度 | O(n) | 需要一個長度為 n 的字元陣列儲存結果 |

---

## 🔗 相關連結

- [LeetCode 題目頁面 (英文)](https://leetcode.com/problems/smallest-string-with-a-given-numeric-value/)
- [LeetCode 題目頁面 (中文)](https://leetcode.cn/problems/smallest-string-with-a-given-numeric-value/)
