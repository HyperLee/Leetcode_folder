# LeetCode 955 — 刪列造序 II (Delete Columns to Make Sorted II)

[原題連結 (LeetCode)](https://leetcode.com/problems/delete-columns-to-make-sorted-ii/) • [中文題目說明](https://leetcode.cn/problems/delete-columns-to-make-sorted-ii/)

---

## 題目描述 🧩

給定一個長度相同的字串陣列 `strs`（共 n 個字串，每個長度 m）。

你可以選擇刪除若干欄（即相同索引位置的字元對每個字串都刪除）。目標是使刪除後的字串陣列依字典序非減（`strs[0] <= strs[1] <= ... <= strs[n-1]`）。請回傳最少需要刪除的欄位數。

---

## 解題概念與出發點 💡

- 我們以 **貪婪 + 從左到右** 的策略檢查每一欄：嘗試保留當前欄，若保留會導致某對相鄰字串失序（lexicographical 比較），則必須刪除該欄。
- 保留某欄後，該欄會把相鄰字串對分出已確定順序的群組（例如已知 `a[i] < a[i+1]`，未來任何欄都不用再比較這對），因此後面的欄只需在尚未確定相對順序的相鄰對內做比較。

這樣的策略能保證在每個欄位作出最少刪除的局部選擇，最終取得最少的刪除數。

---

## 詳細解法與步驟 🔧

1. 用一個長度為 `n` 的字串陣列 `a` 作為每列目前「保留欄」累積而成的字串（初始皆空）。
2. 針對每一欄 `j`（從 0 到 m-1）：

   - 對所有相鄰對 `i` 與 `i+1`，比較 `a[i] + strs[i][j]` 與 `a[i+1] + strs[i+1][j]`。
   - 若任何一對比較結果為 `left > right`，代表如果保留該欄會讓序列失序→必須刪除此欄（答案 +1），然後跳到下一欄。
   - 否則，此欄可保留：把每列的該欄字元正式附加到 `a[i]`，並繼續檢查下一欄。

範例程式（C#）的主體為 `MinDeletionSize`，關鍵比較使用 `StringComparison.Ordinal` 以確保字典序比較：

```csharp
public int MinDeletionSize(string[] strs)
{
    int n = strs.Length, m = strs[0].Length;
    string[] a = new string[n]; // 累積保留欄
    int ans = 0;
    for (int j = 0; j < m; j++) {
        bool deleteColumn = false;
        for (int i = 0; i < n - 1; i++) {
            if (string.Compare(a[i] + strs[i][j], a[i + 1] + strs[i + 1][j], StringComparison.Ordinal) > 0) {
                ans++; deleteColumn = true; break;
            }
        }
        if (!deleteColumn) {
            for (int i = 0; i < n; i++) a[i] += strs[i][j];
        }
    }
    return ans;
}
```

**實作細節與備註：**

- 字串使用累積方式會讓比較成本隨被保留欄數上升；若要進一步優化，可改為只記錄「是否已確定排好序」的布林陣列以避免頻繁串接字串。

---

## 複雜度分析 📊

- 時間複雜度（最壞）：O(n * m^2)（因為字串累積與比較可能導致每次比較成本隨保留欄增加）。
- 空間複雜度：O(n * m)（用來儲存累積字串）。

> 💡 若改用索引比較或布林標記已確定對，就能把時間複雜度改善到 O(n * m)。

---

## 範例與圖示說明 ✅


### 範例 1

Input: `["ca", "bb", "ac"]` → Output: `1`

步驟：

```text
欄 0: (c, b, a) => 不是非減序，須刪除 (標成 X)
欄 1: (a, b, c) => 非減序，保留
結果：刪除 1 欄
```

圖示：

```text
原始矩陣：
 c a
 b b
 a c
 j=0: c, b, a  -> X (刪除)
 j=1: a, b, c  -> ✅ (保留)
```

---

### 範例 2

Input: `["ac","ad","ba","bb"]` → Output: `0`（不需刪列）

說明：

- 欄 0 為 `a, a, b, b`（非減），保留並把序列分成兩個群組 `[ac,ad]`、`[ba,bb]`。
- 欄 1 只需在群組內比較：`c<=d` 與 `a<=b`，皆成立，所以不需刪除任何欄。

圖示：

```text
 a c
 a d
 b a
 b b
 j=0: a,a,b,b -> 保留 (分群)
 j=1: [c<=d] and [a<=b] -> 保留
```

---

## 如何執行 🔍

在此專案中可直接使用 .NET 執行：

```bash
# 執行專案
dotnet run --project leetcode_955/leetcode_955.csproj
```

---

若你想要我把這份 README 翻成英文或加入以圖檔呈現的示意圖（SVG/PNG），我可以幫你產生並加入專案中。🙂
