# LeetCode 3314: Construct the Minimum Bitwise Array I

[![.NET](https://img.shields.io/badge/.NET-10.0-512BD4?style=flat-square)](https://dotnet.microsoft.com/)
[![C#](https://img.shields.io/badge/C%23-14-239120?style=flat-square)](https://docs.microsoft.com/dotnet/csharp/)
[![License](https://img.shields.io/badge/License-MIT-blue?style=flat-square)](LICENSE)

使用位元運算解決 LeetCode 3314 題目的 C# 解法實作。

## 📋 題目描述

給定一個質數陣列 `nums`，對於每個元素 `nums[i]`，找到最小的非負整數 `ans`，使得：

```text
ans | (ans + 1) = nums[i]
```

其中 `|` 表示位元 OR 運算。如果不存在這樣的 `ans`，則返回 `-1`。

### 範例

**範例 1:**

```text
輸入: nums = [2, 3, 5, 7]
輸出: [-1, 1, 4, 3]
```

**範例 2:**

```text
輸入: nums = [11, 13, 31]
輸出: [9, 12, 15]
```

## 💡 解題思路

### 核心觀察

理解這道題的關鍵在於分析 `ans | (ans + 1)` 的位元運算特性：

1. **`ans + 1` 的位元特性**：
   - 將 `ans` 從低位到高位的第一個 `0` 變成 `1`
   - 同時將該 `0` 之前的所有低位 `1` 變為 `0`

2. **`ans | (ans + 1)` 的效果**：
   - 等同於將 `ans` 從低位到高位的第一個 `0` 變成 `1`
   - 其他位元保持不變

### 推導過程

由上述觀察可知，對於給定的 `x = nums[i]`：

- `x` 的二進制中，從低位到高位第一個 `0` 之前的所有連續 `1`，任取一個變為 `0` 都能得到有效的 `ans`
- 為了得到**最小的 `ans`**，我們應該將這些連續 `1` 中**最高位的那個 `1`** 變為 `0`

### 特殊情況

當 `x = 2`（二進制為 `10`）時，最低位的 `0` 之前沒有任何 `1`，因此無法構造有效的 `ans`，返回 `-1`。

## 🔍 演算法流程演示

以 `x = 7`（二進制 `111`）為例：

```text
步驟 1: x = 7 (0b111), d = 1, res = -1
        檢查: (111 & 001) = 001 ≠ 0 → 當前位為 1
        更新: res = 7 - 1 = 6 (0b110)
        移位: d = 2 (0b010)

步驟 2: x = 7 (0b111), d = 2, res = 6
        檢查: (111 & 010) = 010 ≠ 0 → 當前位為 1
        更新: res = 7 - 2 = 5 (0b101)
        移位: d = 4 (0b100)

步驟 3: x = 7 (0b111), d = 4, res = 5
        檢查: (111 & 100) = 100 ≠ 0 → 當前位為 1
        更新: res = 7 - 4 = 3 (0b011)
        移位: d = 8 (0b1000)

步驟 4: x = 7 (0b111), d = 8, res = 3
        檢查: (0111 & 1000) = 0000 = 0 → 遇到第一個 0，停止

結果: ans = 3

驗證: 3 | 4 = 011 | 100 = 111 = 7 ✓
```

以 `x = 2`（二進制 `10`）為例：

```text
步驟 1: x = 2 (0b10), d = 1, res = -1
        檢查: (10 & 01) = 00 = 0 → 最低位為 0，直接停止

結果: ans = -1（無解）
```

## 📊 複雜度分析

| 複雜度     | 分析                                           |
|------------|------------------------------------------------|
| 時間複雜度 | O(n × log(max(nums)))，其中 n 為陣列長度       |
| 空間複雜度 | O(1)，僅使用常數額外空間                       |

## 🚀 快速開始

### 環境需求

- [.NET 10.0 SDK](https://dotnet.microsoft.com/download)

### 執行專案

```bash
# 複製專案
git clone https://github.com/HyperLee/Leetcode_folder.git
cd Leetcode_folder/leetcode_3314

# 建構專案
dotnet build

# 執行程式
dotnet run --project leetcode_3314
```

### 預期輸出

```text
測試 1: nums = [2, 3, 5, 7]
結果: [-1, 1, 4, 3]

測試 2: nums = [11, 13, 31]
結果: [9, 12, 15]

測試 3: nums = [2]
結果: [-1] (2 無解，返回 -1)
```

## 📁 專案結構

```text
leetcode_3314/
├── leetcode_3314.sln          # 解決方案檔案
├── README.md                  # 本文件
└── leetcode_3314/
    ├── leetcode_3314.csproj   # 專案配置
    └── Program.cs             # 主程式與解法實作
```

## 📚 相關資源

- [LeetCode 3314 - Construct the Minimum Bitwise Array I](https://leetcode.com/problems/construct-the-minimum-bitwise-array-i/)
- [位元運算教學](https://en.wikipedia.org/wiki/Bitwise_operation)
