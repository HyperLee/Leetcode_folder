# LeetCode 746 .NET 10 Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrate `leetcode_746` to a documented, deterministic .NET 10 console project and publish it through the repository's Issue #2 workflow.

**Architecture:** The nested console project exposes two pure dynamic-programming APIs. `Main` owns a table-driven acceptance harness, while problem-root documentation and VS Code configuration describe and run the nested project directly.

**Tech Stack:** C# 14, .NET 10 SDK, JSON VS Code configuration, Git, GitHub CLI.

## Global Constraints

- All tracked changes stay under `leetcode_746/`.
- Keep both existing public method signatures.
- Build with zero warnings and zero errors.
- Use one functional commit and squash merge the pull request.
- Do not update Issue #2 until GitHub confirms the PR is merged.

---

### Task 1: Establish isolated baseline and RED

- [ ] Confirm Issue #2 has one unchecked `leetcode_746` entry.
- [ ] Record the legacy `MSB3644` baseline.
- [ ] Add the SDK-style project and 32-check harness without production APIs.
- [ ] Build and confirm `CS0103` reports both missing APIs.

### Task 2: Implement the two pure solutions

- [ ] Implement `MinCostClimbingStairs(int[] cost)` with a DP array.
- [ ] Implement `MinCostClimbingStairs2(int[] cost)` with two rolling states.
- [ ] Build and run until all 32 checks pass with no warnings.

### Task 3: Complete repository artifacts

- [ ] Add problem-root editor, Git, VS Code, contributor, README, and template files.
- [ ] Remove the old solution, App.config, and AssemblyInfo.cs one file at a time.
- [ ] Generate the README transcript from a fresh run.

### Task 4: Verify and review

- [ ] Validate JSON, build, run, transcript, unique fence, whitespace, scope, and legacy absence.
- [ ] Create `feat(leetcode-746): migrate project to .NET 10` as the only commit.
- [ ] Obtain independent read-only review and repair all release-blocking findings.

### Task 5: Publish and close the migration item

- [ ] Push and open a draft PR containing `Refs #2`.
- [ ] Verify head SHA, commit count, changed files, checks, and clean mergeability.
- [ ] Mark ready and squash merge using the verified head SHA.
- [ ] Update only the Issue #2 `leetcode_746` checkbox and read it back.
- [ ] Fast-forward `main` and repeat the complete verification bundle.
