# LeetCode 779 .NET 10 Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `superpowers:subagent-driven-development` or `superpowers:executing-plans` to
> implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Deliver a fully verified and published .NET 10 migration of
`leetcode_779` without changing its public solution contract.

**Architecture:** A nested SDK-style console project contains one pure recursive
solution and a deterministic `Main` acceptance harness. Problem-root config and
documentation make build, run, and debug commands reproducible.

**Tech Stack:** C# 14, .NET 10, VS Code coreclr configuration, GitHub CLI.

## Global Constraints

- All tracked changes stay under `leetcode_779/`.
- Preserve `public static int KthGrammar(int n, int k)`.
- Do not add invalid-input behavior or a formal test project.
- Keep one feature commit relative to `origin/main`.
- Update Issue #2 only after GitHub confirms the squash merge.

## Tasks

- [x] Record the legacy `MSB3644` baseline and establish a `CS0103` RED from the
  harness calling the missing production API.
- [x] Convert the project to SDK-style `net10.0`, remove the three legacy files,
  and implement the approved recursive solution until all ten checks pass.
- [x] Add problem-root configuration, VS Code support, contributor guidance,
  the README template, and the Traditional Chinese teaching README.
- [x] Run JSON, build, run, transcript, fence, whitespace, scope, and legacy-file
  verification; then complete an independent read-only review.
- [x] Create one Conventional Commit relative to `origin/main`.
- [ ] Publish a draft PR, verify and squash merge the reviewed head, update
  exactly the `leetcode_779` Issue #2 line, and rerun the full gate on merged
  `main`.
